package org.phemethyst.squaredswords;

import net.minecraft.network.chat.Component;
import net.minecraft.network.chat.MutableComponent;

/**
 * Translation <em>keys</em> for the mod, plus a small helper for turning them into
 * Components at runtime. Deliberately holds no English text: the copy lives in the
 * hand-authored partials under {@code assets/squaredswords/lang/default/} (interface.json,
 * tooltips.json), which {@link org.phemethyst.squaredswords.datagen.SquaredSwordsLangMerger} merges
 * into the generated {@code en_us.json} during runData. Block, item and creative-tab
 * names are derived and generated by Registrate automatically.
 */
public class Lang {

    public static final String CREATIVE_TAB = "itemGroup." + SquaredSwords.ID;
    public static final String GENERATOR_GENERATING = gui("squaredswords_generator.generating");
    public static final String KINETIC_CONSUMING = gui("squaredswords_kinetic.consuming");
    public static final String RPM = gui("rpm");
    public static final String SU = gui("su");

    private static String gui(String path) {
        return "gui." + SquaredSwords.ID + "." + path;
    }

    public static MutableComponent translate(String key, Object... args) {
        return Component.translatable(key, args);
    }
}
